# Francisco-Briones
# repositorio ejemplo 1 html css git
# Nombre: Francisco Briones
# paso a paso
1.- descargar gir
2.- descargar vsc
3.- crear un directorio
4.- abrir la terminal y probar los cmandos
    git -v para ver la version de git
    git config --global user.name "nombre"
    git config --global user.email "email"
5.- clonar el repositorio
    git clone "link"
6.- guardar cambios
    git add oreoara los archivos
    git commit -m "comentario de avance"
    git push origin main sube los cambios